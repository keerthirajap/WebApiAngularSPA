<div class="modal" id="modalDeleteEncryptedFileConfirm" data-backdrop="static">
    <div class="modal-dialog modal-lg">
        <div class="modal-content" id="modalContentDeleteEncryptedFileConfirm">
            <div class="card  bg-white">
                <div class="card-header" id="cardHeaderModalDeleteEncryptedFileConfirm">
                    <h4 class="pt-1">  Confirmation </h4>
                </div>

                <div class="card-body pl-3 pr-3 pt-3" id="deleteEncryptedFileConfirmBody">
                    <blockquote class="lead ">
                        <p class="mb-0" id="deleteEncryptedFileConfirmText"> </p>
                    </blockquote>
                </div>

                <div class="card-footer text-center">
                    <button type="button" class="btn btn-primary"
                            onclick="fileCryptController.deleteEncryptedFile()">
                        <i class="fas fa-trash-alt"></i> Delete
                    </button>
                    <button type="button" id="btnCancelDeleteEncryptedFileConfirm"
                            onclick="fileCryptController.closeConfirmForDeleteEncryptedFile()"
                            class="btn btn-dark ml-3">
                        <i class="fas fa-times"></i>Cancel
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>


</script>