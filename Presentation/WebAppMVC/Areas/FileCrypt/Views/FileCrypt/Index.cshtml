@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="card">
        <div class="card-header">
            <div class="row">
                <div class="col-6">
                    <h4>  <i class="fas fa-lock"></i>  Encrypted Files </h4>
                </div>
                <div class="col-6">
                    <button type="button" id="btnGetAddUserPartialView" class="btn btn-primary float-right"
                            onclick="fileCryptController.loadUploadFilesPartialView()">
                        <i style="font-size:20px" class="fas fa-file-upload"></i> Upload Files
                    </button>
                </div>
            </div>
        </div>
        <div class="card-body">
            @Html.AjaxGrid(Url.Action("GetEncryptedFilesDetailsAsync"), new { id = "gridGetsUsersAjax" })
        </div>
        @await Html.PartialAsync("_deleteEncryptedFileConfirmPopup.cshtml")
    </div>

<div id="loadUploadFilesPartialView">
</div>

<div id="loadEncryptedFileDownloadHistory">
</div>

<script src="~/jsControllers/fileCrypt/fileCryptController.js"></script>
<script>
        var deleteEncryptedFileActionUrl = "";
         var loadUploadFilesPartialViewActionUrl = '@Url.Action("LoadUploadFilesPartialView", "FileCrypt", new { Area = "FileCrypt" })';
        $('#nav-ItemFileCrypt').addClass('active');
</script>